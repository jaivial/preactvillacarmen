FROM node:22-alpine
WORKDIR /app

COPY package.json package-lock.json ./
RUN npm install --no-audit --no-fund
COPY . .

ENV VITE_PORT=5175
ENV VITE_HMR_PORT=443
EXPOSE 5175

CMD ["sh", "-c", "npm run dev -- --host 0.0.0.0 --port ${VITE_PORT:-5175}"]
